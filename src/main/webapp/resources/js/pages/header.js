function HeaderController($scope, $location) {

    //if($location.$$absUrl.lastIndexOf('/login') > 0){
    //    $scope.activeURL = 'contacts';
    //} else{
    //    if($location.$$absUrl.lastIndexOf('/streetgames') > 0)
    //        $scope.activeURL = 'streetgames';
    //    else
    //        $scope.activeURL = 'home';
    //}
}